{
  "name": "task-consolidator",
  "version": "2.0.0",
  "description": "Consolidate all tasks from your notes into a single panel with sorting, filtering, Kanban board, recurring tasks, natural language dates, and more",
  "main": "dist/main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "tasks",
    "todo",
    "kanban"
  ],
  "author": "QT&E",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.19.8",
    "obsidian": "latest",
    "typescript": "^5.3.2"
  }
}
